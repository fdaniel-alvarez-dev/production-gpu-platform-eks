apiVersion: v1
kind: ResourceQuota
metadata:
  name: inference-prod-quota
  namespace: inference-prod
spec:
  hard:
    requests.nvidia.com/gpu: "8"
    limits.nvidia.com/gpu: "8"
    pods: "50"
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: training-prod-quota
  namespace: training-prod
spec:
  hard:
    requests.nvidia.com/gpu: "32"
    limits.nvidia.com/gpu: "32"
    pods: "100"
